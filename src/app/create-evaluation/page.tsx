"use client";

import React from "react";
import withAuth from "../components/WithAuth";

const CreateEvaluation: React.FC = () => {
  return (
    <div>Create Evaluation Page</div>
  );
};

export default withAuth(CreateEvaluation);
