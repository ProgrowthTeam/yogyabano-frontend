"use client";

import React from "react";
import withAuth from "../components/WithAuth";

const CreateVideo: React.FC = () => {
  return (
    <div>Create Video Page</div>
  );
};

export default withAuth(CreateVideo);
